export const userSelector = state => state.users.user
export const tokenSelector = state => state.users.token

export const productCartSelector = state => state.carts.cart
export const totalSelector = state => state.carts.total

export const productsSelector = state => state.products.products

export const invoiceSelector = state => state.invoices.invoice
